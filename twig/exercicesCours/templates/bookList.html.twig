<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Liste de livres</title>
	</head>
	<body>
    {% for book in books %}
        <div>{{ loop.index }} {{ book.title }} {{ book.author }}</div>
    {% else %}
        <div>Aucun livre disponible</div>
    {% endfor %}
	</body>
</html>
